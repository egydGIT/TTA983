// Exercise: Destroy all four targets with a program using a for loop. You must move 10m to get from one target to another.

extern void object::MassacreLevel1()
{
	
	repeat(4)
	{
		move(10);
		turn(90);
		fire(1);
		wait(1);
		turn(-180);
	}
	
}
